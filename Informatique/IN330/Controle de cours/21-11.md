```
int pile[taille];  tableau contenant la pile
int pp; pointeur de pile
```

estPileVide;
empiler;
depiler;

intPile;


```
int bienParenthésé(char *exp){
	pileInt maPile;
	char c;

	while(*exp != 0){
		if(*exp == '(' || *exp == '['){
			empiler(&maPile, *exp);
		}
		else if(*exp == ']' || *exp == ')'){
			c = depiler(&maPile);
			if(!(c == '(' && *exp == ')' || c == '[' && *exp == ']')){
				return 0;
			}
		}
		exp++;
	}
	return 1;
	
}
```