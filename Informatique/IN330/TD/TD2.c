#include <stdio.h>


void affichagerEnHexadecimal(char mot[]){
    char l = mot[0];
    for(,l == 0, (&l ++)){
        printf("La valeure de la lettre %c est : %x", l, l);
    }
}


int main(){



    return 0;
}